fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
python-dotenv>=1.0.0
httpx>=0.26.0
aiofiles>=23.2.0
reportlab>=4.0.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
aiosmtplib>=3.0.0
# Sentry — initialised only when SENTRY_DSN is set; otherwise the
# app is fully unaware of it. Pulls in the FastAPI integration as
# an extra so the standard middleware hooks register automatically.
sentry-sdk[fastapi]>=2.0.0
